<Window x:Class="SatisfactoryPlanner.Controls.SourceConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configure Item Source" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Configure Source Output:" 
                   Foreground="White" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="0,5">
            <TextBlock Text="Item Name:" Foreground="White" Margin="0,0,0,5"/>
            <TextBox x:Name="ItemNameTextBox" Height="25" Background="#2D2D30" 
                     Foreground="White" BorderBrush="#3F3F46"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,5">
            <TextBlock Text="Items per Minute:" Foreground="White" Margin="0,0,0,5"/>
            <TextBox x:Name="ItemRateTextBox" Height="25" Background="#2D2D30" 
                     Foreground="White" BorderBrush="#3F3F46" Text="60"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Text="Common Items: Iron Ore, Copper Ore, Limestone, Coal, Iron Ingot, Copper Ingot, Screw, Wire, Cable" 
                   Foreground="Gray" FontSize="10" TextWrapping="Wrap" Margin="0,10,0,0"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Height="30" Margin="0,0,10,0" Click="OK_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Height="30" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
